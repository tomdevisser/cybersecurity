# Hardening Windows

## User Account Control

Most home users are logged into a local adminstrator account, without needing that level or privileges. It does increase the risk of system compromise, because malware can infect the system easier with elevated privileges., running in the context of the logged-in user.

UAC stops running sessions with elevated permissions, but prompts a user to confirm permission to run operations that need higher-level privileges.
