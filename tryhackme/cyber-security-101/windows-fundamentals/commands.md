# Commands

## Quick reference

`ipconfig`, to show the network address settings
`cls`, to clear the command screen
`netstat`, to display protocol statistics and current TCP/IP connections
`hostname`, to output the computer name
`net`, to manage network resources
